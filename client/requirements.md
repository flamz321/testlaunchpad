## Packages
framer-motion | Essential for high-end crypto aesthetics, smooth page transitions, and scroll-reveals
date-fns | Formatting timestamps for recent launches

## Notes
- Expecting `/api/launches` to return an array of recent token launches.
- Expecting `/api/stats` to return bot usage statistics.
- Dark mode is enforced by default to match the crypto aesthetic.
